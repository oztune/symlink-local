# symlink-local

Symlinks all the node modules in a given directory together.

## Installation
```
npm install -g oztune/symlink-local
```

## Usage
```
cd ~/my-node-projects
symlink-local .
```

## Options
```
--help
--verbose | -v
--dry-run | -d
```
